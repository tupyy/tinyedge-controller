version: v1

name: dasdaot

description: |
  blabla

selectors:
  namespaces:
    - test
    - ggg
  sets:
    - ttt
    - fff
  devices:
    - toto

secrets:
  - id: nginx-password
    path: nginx
    key: data

resources:
  - $ref: /dep/configmap.yaml
  - $ref: /dep/nginx.yaml
  - $ref: /dep/postgres.yaml
